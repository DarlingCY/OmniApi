# 界面设计规范

## 圆角

- 界面所有常规圆角统一为 **8px**，亮色与暗色主题保持一致。
- 适用范围包括按钮、标签、输入框、下拉框、日期选择器、分页项、卡片、列表条目、提示框和弹层等具有圆角的矩形元素。
- `apps/web/src/styles.css` 中的 `--radius-control`、`--radius-item`、`--radius-container` 均为 `8px`，新增样式复用这些变量，不另设 2px、4px 或 6px 圆角。
- Arco 的 `--border-radius-small`、`--border-radius-medium`、`--border-radius-large` 在 `body` 层统一映射到上述变量，确保挂载到 `body` 的弹层也遵守规范。
- 圆形语义元素（状态圆点、单选指示器、开关滑块等）保留圆形；开关轨道保留组件原生形状。无圆角的结构边界、拼接控件内部接缝不强制添加圆角。
- 修改后通过开发服务器检查浏览器计算样式，确认亮暗主题及相关弹层实际使用 `8px`，不能只依据源码变量判断生效。